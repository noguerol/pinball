# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Local pi project config
.pi/

# punched-memory — never commit project-local memory
pi.md
pi.md.local
.punched-memory.json
.punched-memory/

# t-plan: plan files are private runtime state — never commit or publish them
plan_*_[0-9a-zA-Z]*.md
plan.md
# t-plan: private runtime state — never commit or publish
plan_*.md
